{
  "name": "mensajes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "dev:watch": "nodemon --watch src --ext js,json src/index.js",
    "test": "node --check src/index.js && echo 'Syntax check passed!'",
    "lint": "echo 'No linter configured. Consider adding ESLint.'",
    "docker:build": "docker build -t whatsapp-api .",
    "docker:build:dev": "docker build -f Dockerfile.dev -t whatsapp-api:dev .",
    "docker:run": "docker run -p 3001:3001 whatsapp-api",
    "docker:run:dev": "docker run -p 3002:3001 whatsapp-api:dev",
    "docker:compose:up": "docker-compose up -d",
    "docker:compose:up:dev": "docker-compose --profile dev up -d",
    "docker:compose:down": "docker-compose down",
    "docker:compose:logs": "docker-compose logs -f",
    "docker:clean": "docker system prune -f && docker volume prune -f"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.10.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^24.19.0",
    "puppeteer-core": "^23.8.0",
    "qrcode-terminal": "^0.12.0",
    "whatsapp-web.js": "^1.34.0"
  }
}
